import Client from '../Client.js'

class Chat {
  static sendJoinMessage (nickName) {
    let str
    /* Style the string
    client.call('ChatSendServerMessage',
      [{ string: str }]) */
  }

  static newLocalRecord (login) {

  }
}

export default Chat

export class Chat {
  static sendJoinMessage (nickName: string) {
    let str
    /* Style the string
    client.call('ChatSendServerMessage',
      [{ string: str }]) */
  }

  static newLocalRecord (login: string) {

  }
}
